let totalAllumettes = 50;
let joueurs = [];
let Nomjoueur = "???";
let nombreJoueurs = Number(prompt("bien de joueurs von participer ?"));
let joueurActuelIndex = 0;

function priseAllumettes(number) {
  if (isNaN(priseAllumettes > 6 && priseAllumettes < 1)) {
    console.log("tu doit prendre entre 1 et 6");
  }

  while (totalAllumettes > 0) {
    let joueurActuel = joueurs["joueurActuelIndex"];
    let priseAllumettes = Number(
      prompt(
        `combiens d'allumette veut tu prendre ${joueurActuelIndex} ? il reste ${totalAllumettes}`
      )
    );
  }
}
priseAllumettes();
